import { StyleSheet, Dimensions } from 'react-native';
import { assetColors, assetTypography } from '../config/styles';

const styles = StyleSheet.create({
    container: {
        backgroundColor: 'white'
    },
    image: {
        height: 100,
        width: 100,
        borderRadius: 50
    }
});
export default speakerStyles;
